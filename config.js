const token = require('./token');

module.exports = {
	token: token,
	welcomeMessage: `Svg minification is here! Under the hood it's use svgo (https://github.com/svg/svgo). Send to it svg in file or markup.`
};